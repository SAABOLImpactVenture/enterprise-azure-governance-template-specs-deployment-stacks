{
  "name": "smart-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for enterprise governance",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "node scripts/deploy.js",
    "deploy:testnet": "hardhat run smart-contracts/scripts/deploy.js --network goerli"
  },
  "keywords": [
    "ethereum",
    "hardhat",
    "solidity",
    "smart-contracts"
  ],
  "author": "GEP-V",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-network-helpers": "^1.0.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "chai": "^4.2.0",
    "ethers": "^6.0.0",
    "hardhat": "^2.19.4",
    "hardhat-gas-reporter": "^1.0.8",
    "solidity-coverage": "^0.8.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "dotenv": "^16.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "resolutions": {
    "cookie": "^0.7.0"
  }
}
